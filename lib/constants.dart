import 'package:flutter/material.dart';

class Constants {
  static const String appName = 'PhotoCode';
  static const String logoTag = 'com.photocode.photocodemobile.logo';
  static const String titleTag = 'com.photocode.photocodemobile.title';
  static const Color accentColor = Color.fromRGBO(13, 212, 127, 1);
  static const Color textColor = Colors.white;
  static const Color backgroundColor = Color.fromRGBO(33, 38, 44, 1);
  static const Color barBackgroundColor = Colors.black;
}
